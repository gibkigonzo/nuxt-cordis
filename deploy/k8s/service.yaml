apiVersion: v1
kind: Service
metadata:
  name: shop
  namespace: shop
  labels:
    app: shop
spec:
  selector:
    app: shop
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
