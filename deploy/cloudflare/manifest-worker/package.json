{
  "name": "manifest-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker skladajacy wpisy KV w endpoint /manifest.json dla @shop/remote-sync. Standalone (poza glownym pnpm workspace) - inny runtime docelowy (Workers, nie Node).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "wrangler": "^4.0.0"
  }
}
